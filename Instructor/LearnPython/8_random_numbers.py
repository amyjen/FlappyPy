from random import randint #as alias # you have to import and can use the import as an alias

print(randint(1,10)) #prints random number

same_randomint = randint(1,10) #prints same random number twice 
print(same_randomint)
print(same_randomint)

#print(alais(0,10))
