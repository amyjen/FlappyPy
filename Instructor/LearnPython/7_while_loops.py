pie_slices = 8 #creates varialbe effectively the amount of times we will loop

while pie_slices > 0: #will loop as long as condition is true
    print("There's", pie_slices, "slice(s) left.  I guess I'll eat one then.")
    pie_slices -= 1
print("There's none left.  I'm so full") #exits and runs proceding lines of code
